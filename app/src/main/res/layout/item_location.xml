<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:id="@+id/location_name"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:fontFamily="sans-serif-condensed" />
    <LinearLayout
        android:id="@+id/colors_indicator_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true">
        <ImageView
            android:layout_width="@dimen/color_indicator_size"
            android:layout_height="@dimen/color_indicator_size"
            android:background="@drawable/bg_color_indicator" />
        <ImageView
            android:layout_width="@dimen/color_indicator_size"
            android:layout_height="@dimen/color_indicator_size"
            android:background="@drawable/text_color_indicator"/>
    </LinearLayout>
</RelativeLayout>